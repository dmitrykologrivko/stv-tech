import TableHeader from '../Table/TableHeader.jsx';

const ThemedTableHeader = ({ children }) => {
  return (
    <TableHeader>
      {children}
    </TableHeader>
  );
};

export default ThemedTableHeader;
